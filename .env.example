# Admin panel credentials (optional - used only when set)
# Create .env and add these to override defaults. Keep .env out of version control!
# VITE_ADMIN_USER=your_admin_username
# VITE_ADMIN_PASS=your_secure_password